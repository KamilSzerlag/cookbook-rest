package com.kszerlag.cookbookrest.util;

import javax.persistence.MappedSuperclass;

/**
 * @author kszerlag
 *
 * Base class for
 * all Entities contains
 * id as PK
 */
@MappedSuperclass
public class BaseEntity {

    private
}
